query {
  categories(treeLevel: 1) {
    id, 
    name,
    slug
    children {
      id,
      name,
      slug
    }
  }
}
Esta versión solo obtiene la categoria numero 3, la modifique
para mostrar todas las categorias existentes.

departments={data?.categories[3]?.children}
